version: '3.8'

services:
  hub:
    image: nginx:alpine
    container_name: hub
    restart: unless-stopped
    networks:
      - traefik_net
    volumes:
      - ./html:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    deploy:
      resources:
        limits:
          cpus: '0.2'
          memory: 128M
        reservations:
          cpus: '0.1'
          memory: 64M
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.hub.rule=Host(`hub.millyweb.com`)"
      - "traefik.http.routers.hub.entrypoints=websecure"
      - "traefik.http.routers.hub.tls.certresolver=ledns"
      - "traefik.http.services.hub.loadbalancer.server.port=80"
      - "traefik.http.routers.hub.middlewares=default-headers"

networks:
  traefik_net:
    external: true
